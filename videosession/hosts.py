# from django.conf import settings
# from django_hosts import patterns, host
#
# host_patterns = patterns('',
#     host(r'https://d95a9d4bc565.ngrok.io/random/', settings.ROOT_URLCONF, name='https://d95a9d4bc565.ngrok.io/random/'),  # <-- The `name` we used to in the `DEFAULT_HOST` setting
# )